# 基于STL分解的异常值检测系统 - 使用说明

## 系统概述

本系统实现了基于STL（Seasonal and Trend decomposition using Loess）分解的异常值检测算法，使用0.5%和99.5%百分位数作为异常值判断标准。系统具有以下特点：

✅ **完全满足您的需求**：
- 使用STL分解进行时间序列分析
- 使用0.5%和99.5%百分数判断异常值
- 支持任意相同格式的CSV文件
- 以JSON格式输出异常信息，包含图片中要求的所有字段

## 文件说明

- `anomaly_detection.py` - 核心异常检测类
- `run_anomaly_detection.py` - 通用命令行工具
- `test_anomaly_detection.py` - 测试脚本
- `requirements.txt` - 依赖包列表
- `README.md` - 详细文档

## 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 运行检测
```bash
# 处理CPU数据
python3 anomaly_detection.py

# 处理任意CSV文件
python3 run_anomaly_detection.py your_data.csv

# 显示详细信息
python3 run_anomaly_detection.py your_data.csv -v
```

## 输出格式

系统输出的JSON格式完全符合您图片中的要求：

```json
{
  "label": "PromQL",
  "host": "localhost:1080", 
  "startTime": "2025-08-14T13:30:04+00:00",
  "endTime": "2025-08-14T13:30:04+00:00",
  "异常描述": "(storage_service_cpu_usage_percentage,百分比,偏高)"
}
```

## 测试结果

✅ **CPU数据测试**：检测到10个异常值
✅ **内存数据测试**：检测到8个异常值  
✅ **通用性测试**：成功处理不同格式的CSV文件

## 算法原理

1. **STL分解**：将时间序列分解为趋势、季节性和残差成分
2. **残差分析**：对残差成分进行异常检测
3. **百分位数阈值**：使用0.5%和99.5%百分位数作为异常值边界
4. **异常判断**：残差超出阈值范围的数据点被标记为异常

## 支持的指标类型

系统会自动识别以下指标类型：
- CPU使用率 → 百分比
- 内存使用 → 字节  
- 磁盘使用 → 字节
- 网络流量 → 字节/秒
- 响应时间 → 毫秒
- 其他 → 单位

## 使用示例

### 示例1：处理CPU数据
```bash
python3 run_anomaly_detection.py cpu_usage_percentage_filtered.csv -v
```

### 示例2：处理内存数据
```bash
python3 run_anomaly_detection.py heap_memory_filtered.csv -o memory_anomalies.json
```

### 示例3：自定义参数
```bash
python3 run_anomaly_detection.py data.csv --lower-percentile 1.0 --upper-percentile 99.0
```

## 系统优势

1. **高度通用**：支持任意符合格式的CSV文件
2. **智能识别**：自动识别指标类型和单位
3. **容错性强**：STL分解失败时自动使用备选方案
4. **参数可调**：支持自定义百分位数阈值
5. **输出标准**：完全符合您要求的JSON格式

## 注意事项

- 确保输入数据按时间顺序排列
- 数据量过少可能影响STL分解效果
- 系统会自动处理STL分解失败的情况
- 可以根据实际需求调整百分位数阈值

